# CLI Reference

Earthquake is operated via the CLI by running the `earthquake run` command. To
stop it, press `ctrl-c`. You may need to wait a bit as earthquake completes
cleanup tasks.

However, Earthquake has more commands:

{{#include ./SUMMARY.md}}
